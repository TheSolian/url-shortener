'use client';

import { WorkInProgress } from '@/components/debug/work-in-progress';

export default function Page() {
    return (
        <div>
            <WorkInProgress />
        </div>
    );
}
